export enum log {
  in = "loggedIn",
  out = "loggedOut",
  reg = 'register'
}

export enum localStorageItems {
  name = "userName",
  status = "status"
}

export enum sideBarState {
  open = "open",
  close = "close"
}